package com.example.movie.utils

import com.example.movie.data.Show
import com.example.movie.domain.Movie

object DataDummy {
    fun generateMovies(): List<Movie> {
        return ArrayList()
    }

    fun generateShows(): List<Show> {
        return ArrayList()
    }
}